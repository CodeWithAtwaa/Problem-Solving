#include <iostream>
#define ll long long
using namespace std;

int main()
{
    ll a, b, c, d, res;
    cin >> a >> b >> c >> d;
    res = (a * b) - (c * d);
    cout << "Difference = " << res << endl;
}